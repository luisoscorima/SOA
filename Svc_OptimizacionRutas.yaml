openapi: 3.0.0

info:
  title: Svc_OptimizacionRutas - API de TMS
  description: |
    Servicio encargado de optimizar rutas de transporte a partir de datos de despacho, direcciones, ventanas horarias y restricciones operativas.

    ---

    **Información Técnica del Servicio:**

    * **Protocolo:** REST / HTTPS / JSON

    * **Puerto del protocolo:** 8081 (Entorno Local)

    * **Tipo de servicio:** Tarea

    * **Justificación SOA:** Autonomía, ya que encapsula una lógica compleja e independiente de optimización. Asincronismo, porque puede ejecutarse en segundo plano.

    ---
  version: 1.0.0

servers:
  - url: http://localhost:8081/api/tms
    description: Servidor Local de Desarrollo (Puerto 8081)

paths:
  /rutas/optimizar:
    post:
      summary: Optimizar ruta de transporte
      description: Recibe datos de despacho y genera una ruta optimizada para la distribución de mercancía.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/OptimizacionRutasRequest'
            example:
              id_despacho: "DESP-000125"
              id_vehiculo: "VEH-000321"
              punto_origen: "ALM_LIMA"
              destinos:
                - id_destino: "CLI-001"
                  direccion: "Av. Principal 123, Lima"
                  ventana_horaria: "09:00-12:00"
                - id_destino: "CLI-002"
                  direccion: "Av. Industrial 456, Lima"
                  ventana_horaria: "14:00-17:00"
      responses:
        '202':
          description: Solicitud de optimización recibida correctamente
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/OptimizacionRutasResponseExito'
        '400':
          description: Datos de entrada inválidos
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/OptimizacionRutasResponseError400'
        '409':
          description: La ruta ya fue optimizada previamente
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/OptimizacionRutasResponseError409'

components:
  schemas:
    OptimizacionRutasRequest:
      type: object
      required:
        - id_despacho
        - punto_origen
        - destinos
      properties:
        id_despacho:
          type: string
          maxLength: 50
          description: "Código único del despacho para el cual se optimizará la ruta."
          example: "DESP-000125"
        id_vehiculo:
          type: string
          maxLength: 50
          description: "Código del vehículo asignado para el transporte."
          example: "VEH-000321"
        punto_origen:
          type: string
          maxLength: 100
          description: "Punto de origen de la ruta de distribución."
          example: "ALM_LIMA"
        destinos:
          type: array
          description: "Listado de destinos que deberán considerarse en la optimización."
          items:
            type: object
            properties:
              id_destino:
                type: string
                maxLength: 50
                example: "CLI-001"
              direccion:
                type: string
                maxLength: 200
                example: "Av. Principal 123, Lima"
              ventana_horaria:
                type: string
                maxLength: 50
                example: "09:00-12:00"

    OptimizacionRutasResponseExito:
      type: object
      properties:
        ok:
          type: boolean
          example: true
        id_ruta:
          type: string
          maxLength: 50
          example: "RUTA-000789"
        estado:
          type: string
          maxLength: 30
          example: "en_proceso"
        mensaje:
          type: string
          example: "Solicitud de optimización de ruta recibida correctamente"

    OptimizacionRutasResponseError400:
      type: object
      properties:
        ok:
          type: boolean
          example: false
        error:
          type: string
          maxLength: 100
          example: "DATOS_INVALIDOS"
        detalle:
          type: string
          maxLength: 250
          example: "La lista de destinos no puede estar vacía."

    OptimizacionRutasResponseError409:
      type: object
      properties:
        ok:
          type: boolean
          example: false
        error:
          type: string
          maxLength: 100
          example: "RUTA_YA_OPTIMIZADA"
        detalle:
          type: string
          maxLength: 250
          example: "El despacho enviado ya cuenta con una ruta optimizada."